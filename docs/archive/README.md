# Archived Design Notes

This directory keeps historical planning documents that are no longer the
current entry point. Use `docs/architecture.md`, `docs/design/*.md`,
`docs/project_plan.md`, and `docs/work_rules.md` for active guidance.

Documents in this directory are not implementation contracts. If an archived
note conflicts with an active design document, the active design document wins.

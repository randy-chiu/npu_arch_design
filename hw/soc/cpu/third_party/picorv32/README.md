# PicoRV32

This directory vendors the PicoRV32 RTL used as the first RV32 CPU core for
minimal SoC bring-up.

Source:

```text
https://github.com/YosysHQ/picorv32
```

The vendored `picorv32.v` file carries its upstream license header.

# KV Cache RTL

Planned Transformer NPU v1 location for KV-cache streamer work.

V1 starts with spec and counters only. A real KV-cache streamer should be added
only after decode bytes/token and PPA evidence justify the hardware path.

# Temporary Verification Fixtures

Generated RTL and SoC simulation fixture hex files currently live under
`build/rtl_fixture`.

This directory is reserved for checked-in fixture definitions or metadata if
the verification flow needs them later. Generated artifacts should stay under
`build`.
